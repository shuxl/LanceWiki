# 授权机制

## 重点内容
- Spring Security授权流程和核心组件
- 基于角色和权限的访问控制
- 方法级安全和表达式语言
- 自定义权限评估器
- 动态权限控制
- 细粒度授权和数据级安全

## 授权机制概念或介绍

授权机制是Spring Security框架中负责控制用户访问权限的核心功能。它基于用户的身份信息和权限，决定用户是否可以访问特定的资源或执行特定的操作。授权机制通过AccessDecisionManager、AccessDecisionVoter等组件实现灵活的权限控制策略。

### 授权机制特点
1. **多层次控制**：支持URL级、方法级、数据级的权限控制
2. **灵活策略**：支持基于角色、权限、表达式的多种授权策略
3. **动态权限**：支持运行时动态调整权限配置
4. **细粒度控制**：支持对单个资源或操作的精确权限控制
5. **可扩展架构**：提供丰富的扩展点，支持自定义授权逻辑

## 底层原理

### 关键类和接口

#### AccessDecisionManager接口
```java
public interface AccessDecisionManager {
    
    // 决定是否允许访问
    void decide(Authentication authentication, Object object, 
                Collection<ConfigAttribute> configAttributes) throws AccessDeniedException, InsufficientAuthenticationException;
    
    // 是否支持指定的配置属性
    boolean supports(ConfigAttribute attribute);
    
    // 是否支持指定的安全对象类型
    boolean supports(Class<?> clazz);
}
```

#### AccessDecisionVoter接口
```java
public interface AccessDecisionVoter<S> {
    
    // 投票结果常量
    int ACCESS_GRANTED = 1;
    int ACCESS_ABSTAIN = 0;
    int ACCESS_DENIED = -1;
    
    // 投票决定
    int vote(Authentication authentication, S object, Collection<ConfigAttribute> attributes);
    
    // 是否支持指定的配置属性
    boolean supports(ConfigAttribute attribute);
    
    // 是否支持指定的安全对象类型
    boolean supports(Class<?> clazz);
}
```

#### SecurityMetadataSource接口
```java
public interface SecurityMetadataSource {
    
    // 获取安全对象所需的配置属性
    Collection<ConfigAttribute> getAttributes(Object object) throws IllegalArgumentException;
    
    // 获取所有支持的配置属性
    Collection<ConfigAttribute> getAllConfigAttributes();
    
    // 是否支持指定的安全对象类型
    boolean supports(Class<?> clazz);
}
```

### 关键类图

```
HttpServletRequest
    ↓
FilterSecurityInterceptor
    ↓
SecurityMetadataSource
    ↓
AccessDecisionManager
    ↓
AccessDecisionVoter
    ↓
Authentication
    ↓
Authorization Decision
```

### 核心代码讲解

#### 1. 访问决策管理器
```java
@Component
public class CustomAccessDecisionManager implements AccessDecisionManager {
    
    private List<AccessDecisionVoter<?>> decisionVoters;
    
    public CustomAccessDecisionManager(List<AccessDecisionVoter<?>> decisionVoters) {
        this.decisionVoters = decisionVoters;
    }
    
    @Override
    public void decide(Authentication authentication, Object object, 
                      Collection<ConfigAttribute> configAttributes) throws AccessDeniedException, InsufficientAuthenticationException {
        
        int deny = 0;
        int grant = 0;
        
        for (AccessDecisionVoter voter : decisionVoters) {
            int result = voter.vote(authentication, object, configAttributes);
            
            switch (result) {
                case AccessDecisionVoter.ACCESS_GRANTED:
                    grant++;
                    break;
                case AccessDecisionVoter.ACCESS_DENIED:
                    deny++;
                    break;
                default:
                    break;
            }
        }
        
        if (deny > 0) {
            throw new AccessDeniedException("Access denied");
        }
        
        if (grant == 0) {
            throw new AccessDeniedException("No voters granted access");
        }
    }
    
    @Override
    public boolean supports(ConfigAttribute attribute) {
        return decisionVoters.stream().anyMatch(voter -> voter.supports(attribute));
    }
    
    @Override
    public boolean supports(Class<?> clazz) {
        return decisionVoters.stream().anyMatch(voter -> voter.supports(clazz));
    }
}
```

#### 2. 自定义投票器
```java
@Component
public class CustomAccessDecisionVoter implements AccessDecisionVoter<Object> {
    
    @Override
    public int vote(Authentication authentication, Object object, Collection<ConfigAttribute> attributes) {
        if (authentication == null) {
            return ACCESS_DENIED;
        }
        
        for (ConfigAttribute attribute : attributes) {
            if (this.supports(attribute)) {
                String requiredRole = attribute.getAttribute();
                
                for (GrantedAuthority authority : authentication.getAuthorities()) {
                    if (authority.getAuthority().equals(requiredRole)) {
                        return ACCESS_GRANTED;
                    }
                }
            }
        }
        
        return ACCESS_ABSTAIN;
    }
    
    @Override
    public boolean supports(ConfigAttribute attribute) {
        return attribute.getAttribute().startsWith("ROLE_");
    }
    
    @Override
    public boolean supports(Class<?> clazz) {
        return true;
    }
}
```

#### 3. 安全元数据源
```java
@Component
public class CustomSecurityMetadataSource implements SecurityMetadataSource {
    
    private final Map<String, Collection<ConfigAttribute>> urlRoleMap = new HashMap<>();
    
    public CustomSecurityMetadataSource() {
        // 初始化URL-角色映射
        urlRoleMap.put("/admin/**", Arrays.asList(new SecurityConfig("ROLE_ADMIN")));
        urlRoleMap.put("/user/**", Arrays.asList(new SecurityConfig("ROLE_USER")));
        urlRoleMap.put("/api/**", Arrays.asList(new SecurityConfig("ROLE_API_USER")));
    }
    
    @Override
    public Collection<ConfigAttribute> getAttributes(Object object) throws IllegalArgumentException {
        if (object instanceof FilterInvocation) {
            FilterInvocation fi = (FilterInvocation) object;
            String url = fi.getRequestUrl();
            
            for (Map.Entry<String, Collection<ConfigAttribute>> entry : urlRoleMap.entrySet()) {
                if (pathMatcher.match(entry.getKey(), url)) {
                    return entry.getValue();
                }
            }
        }
        
        return Collections.emptyList();
    }
    
    @Override
    public Collection<ConfigAttribute> getAllConfigAttributes() {
        return urlRoleMap.values().stream()
            .flatMap(Collection::stream)
            .collect(Collectors.toList());
    }
    
    @Override
    public boolean supports(Class<?> clazz) {
        return FilterInvocation.class.isAssignableFrom(clazz);
    }
}
```

### 设计思想

#### 1. 策略模式
通过AccessDecisionVoter接口实现不同的授权策略，支持多种投票机制。

#### 2. 责任链模式
通过AccessDecisionManager协调多个AccessDecisionVoter，实现授权决策。

#### 3. 元数据驱动
通过SecurityMetadataSource提供安全配置信息，实现配置与逻辑分离。

#### 4. 可扩展架构
提供丰富的扩展点，支持自定义授权逻辑和投票机制。

## 基于角色的访问控制

### URL级授权配置
```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(authz -> authz
                .requestMatchers("/public/**").permitAll()
                .requestMatchers("/admin/**").hasRole("ADMIN")
                .requestMatchers("/user/**").hasAnyRole("USER", "ADMIN")
                .requestMatchers("/api/**").hasRole("API_USER")
                .requestMatchers("/manager/**").hasAuthority("MANAGE_USERS")
                .anyRequest().authenticated()
            );
        
        return http.build();
    }
}
```

### 方法级授权
```java
@Configuration
@EnableWebSecurity
@EnableMethodSecurity
public class SecurityConfig {
    
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        return http.build();
    }
}
```

### 服务层授权
```java
@Service
public class UserService {
    
    @PreAuthorize("hasRole('ADMIN')")
    public List<User> getAllUsers() {
        return userRepository.findAll();
    }
    
    @PreAuthorize("hasRole('USER') and #user.id == authentication.principal.id")
    public User updateUser(User user) {
        return userRepository.save(user);
    }
    
    @PreAuthorize("hasAuthority('READ_USER')")
    public User getUserById(Long id) {
        return userRepository.findById(id).orElse(null);
    }
    
    @PostAuthorize("returnObject.owner == authentication.name")
    public User getUserProfile() {
        return getCurrentUser();
    }
    
    @PreAuthorize("hasRole('ADMIN') or #user.department == authentication.principal.department")
    public void deleteUser(User user) {
        userRepository.delete(user);
    }
}
```

## 表达式语言

### SpEL表达式示例
```java
@Service
public class DocumentService {
    
    @PreAuthorize("hasRole('ADMIN') or hasRole('MANAGER')")
    public List<Document> getAllDocuments() {
        return documentRepository.findAll();
    }
    
    @PreAuthorize("hasRole('ADMIN') or #document.owner == authentication.name")
    public Document getDocument(Document document) {
        return documentRepository.findById(document.getId()).orElse(null);
    }
    
    @PreAuthorize("hasRole('ADMIN') or (hasRole('USER') and #document.isPublic)")
    public Document getDocumentById(Long id) {
        return documentRepository.findById(id).orElse(null);
    }
    
    @PreAuthorize("@documentService.canAccess(#document, authentication)")
    public void updateDocument(Document document) {
        documentRepository.save(document);
    }
    
    @PreAuthorize("hasRole('ADMIN') and #user.department == 'IT'")
    public void assignRole(User user, String role) {
        userService.assignRole(user, role);
    }
}
```

### 自定义表达式
```java
@Component("documentService")
public class DocumentService {
    
    public boolean canAccess(Document document, Authentication authentication) {
        if (hasRole(authentication, "ADMIN")) {
            return true;
        }
        
        if (hasRole(authentication, "USER") && document.isPublic()) {
            return true;
        }
        
        return document.getOwner().equals(authentication.getName());
    }
    
    private boolean hasRole(Authentication authentication, String role) {
        return authentication.getAuthorities().stream()
            .anyMatch(authority -> authority.getAuthority().equals("ROLE_" + role));
    }
}
```

## 自定义权限评估

### 权限评估器
```java
@Component
public class CustomPermissionEvaluator implements PermissionEvaluator {
    
    @Override
    public boolean hasPermission(Authentication authentication, Object targetDomainObject, Object permission) {
        if (authentication == null || targetDomainObject == null || !(permission instanceof String)) {
            return false;
        }
        
        String targetType = targetDomainObject.getClass().getSimpleName().toUpperCase();
        return hasPrivilege(authentication, targetType, permission.toString().toUpperCase());
    }
    
    @Override
    public boolean hasPermission(Authentication authentication, Serializable targetId, String targetType, Object permission) {
        if (authentication == null || targetType == null || !(permission instanceof String)) {
            return false;
        }
        
        return hasPrivilege(authentication, targetType.toUpperCase(), permission.toString().toUpperCase());
    }
    
    private boolean hasPrivilege(Authentication authentication, String targetType, String permission) {
        for (GrantedAuthority grantedAuth : authentication.getAuthorities()) {
            if (grantedAuth.getAuthority().startsWith(targetType) && 
                grantedAuth.getAuthority().contains(permission)) {
                return true;
            }
        }
        return false;
    }
}
```

### 配置权限评估器
```java
@Configuration
@EnableMethodSecurity
public class MethodSecurityConfig {
    
    @Bean
    public MethodSecurityExpressionHandler methodSecurityExpressionHandler() {
        DefaultMethodSecurityExpressionHandler expressionHandler = new DefaultMethodSecurityExpressionHandler();
        expressionHandler.setPermissionEvaluator(new CustomPermissionEvaluator());
        return expressionHandler;
    }
}
```

## 动态权限控制

### 动态权限加载
```java
@Service
public class DynamicPermissionService {
    
    @Autowired
    private PermissionRepository permissionRepository;
    
    public Collection<ConfigAttribute> getAttributes(String url) {
        List<Permission> permissions = permissionRepository.findByUrl(url);
        
        return permissions.stream()
            .map(permission -> new SecurityConfig(permission.getRole()))
            .collect(Collectors.toList());
    }
    
    public void refreshPermissions() {
        // 刷新权限缓存
        SecurityContextHolder.clearContext();
    }
}
```

### 动态权限投票器
```java
@Component
public class DynamicPermissionVoter implements AccessDecisionVoter<Object> {
    
    @Autowired
    private DynamicPermissionService permissionService;
    
    @Override
    public int vote(Authentication authentication, Object object, Collection<ConfigAttribute> attributes) {
        if (authentication == null) {
            return ACCESS_DENIED;
        }
        
        if (object instanceof FilterInvocation) {
            FilterInvocation fi = (FilterInvocation) object;
            String url = fi.getRequestUrl();
            
            Collection<ConfigAttribute> requiredAttributes = permissionService.getAttributes(url);
            
            for (ConfigAttribute attribute : requiredAttributes) {
                for (GrantedAuthority authority : authentication.getAuthorities()) {
                    if (authority.getAuthority().equals(attribute.getAttribute())) {
                        return ACCESS_GRANTED;
                    }
                }
            }
        }
        
        return ACCESS_ABSTAIN;
    }
    
    @Override
    public boolean supports(ConfigAttribute attribute) {
        return true;
    }
    
    @Override
    public boolean supports(Class<?> clazz) {
        return FilterInvocation.class.isAssignableFrom(clazz);
    }
}
```

## 细粒度授权

### 数据级权限控制
```java
@Service
public class DataPermissionService {
    
    @Autowired
    private UserRepository userRepository;
    
    @PreAuthorize("hasRole('ADMIN') or #user.department == authentication.principal.department")
    public List<User> getUsersByDepartment(User currentUser) {
        if (hasRole(currentUser, "ADMIN")) {
            return userRepository.findAll();
        } else {
            return userRepository.findByDepartment(currentUser.getDepartment());
        }
    }
    
    @PreAuthorize("hasRole('ADMIN') or #document.owner == authentication.name")
    public Document getDocument(Document document) {
        return documentRepository.findById(document.getId()).orElse(null);
    }
    
    @PreAuthorize("hasRole('ADMIN') or @dataPermissionService.canAccessProject(#project, authentication)")
    public Project getProject(Project project) {
        return projectRepository.findById(project.getId()).orElse(null);
    }
    
    public boolean canAccessProject(Project project, Authentication authentication) {
        User user = (User) authentication.getPrincipal();
        
        // 检查用户是否是项目成员
        return project.getMembers().contains(user) || 
               project.getOwner().equals(user);
    }
}
```

### 字段级权限控制
```java
@Component
public class FieldLevelSecurityService {
    
    public <T> T filterSensitiveFields(T object, Authentication authentication) {
        if (hasRole(authentication, "ADMIN")) {
            return object;
        }
        
        // 根据用户权限过滤敏感字段
        return filterFields(object, authentication);
    }
    
    private <T> T filterFields(T object, Authentication authentication) {
        // 实现字段过滤逻辑
        return object;
    }
    
    private boolean hasRole(Authentication authentication, String role) {
        return authentication.getAuthorities().stream()
            .anyMatch(authority -> authority.getAuthority().equals("ROLE_" + role));
    }
}
```

## 授权异常处理

### 自定义访问拒绝处理器
```java
@Component
public class CustomAccessDeniedHandler implements AccessDeniedHandler {
    
    @Override
    public void handle(HttpServletRequest request, 
                      HttpServletResponse response, 
                      AccessDeniedException accessDeniedException) throws IOException, ServletException {
        
        response.setStatus(HttpServletResponse.SC_FORBIDDEN);
        
        if (isAjaxRequest(request)) {
            response.setContentType("application/json");
            response.getWriter().write("{\"error\":\"Access denied\"}");
        } else {
            response.sendRedirect("/access-denied");
        }
    }
    
    private boolean isAjaxRequest(HttpServletRequest request) {
        return "XMLHttpRequest".equals(request.getHeader("X-Requested-With"));
    }
}
```

### 方法级异常处理
```java
@Service
public class SecureService {
    
    @PreAuthorize("hasRole('ADMIN')")
    public void adminOnlyMethod() {
        // 只有管理员可以访问
    }
    
    @PreAuthorize("hasRole('USER')")
    public void userMethod() {
        // 用户和管理员都可以访问
    }
    
    @PostAuthorize("returnObject.owner == authentication.name")
    public User getCurrentUserProfile() {
        return getCurrentUser();
    }
    
    @PreAuthorize("hasRole('ADMIN') or #user.id == authentication.principal.id")
    public void updateUser(User user) {
        // 管理员可以更新任何用户，普通用户只能更新自己
    }
}
```

## 授权机制关联的其它知识

### 1. Spring Security核心
- [Spring Security基础](0801-Spring%20Security基础.md)
- [认证机制](0802-认证机制.md)
- [安全配置](0804-安全配置.md)

### 2. 权限管理
- [RBAC权限模型](../../500-基础理论/权限管理/RBAC权限模型.md)
- [ABAC权限模型](../../500-基础理论/权限管理/ABAC权限模型.md)
- [权限设计模式](../../500-基础理论/权限管理/权限设计模式.md)

### 3. 安全技术
- [访问控制](../../500-基础理论/安全技术/访问控制.md)
- [权限验证](../../500-基础理论/安全技术/权限验证.md)
- [安全审计](../../500-基础理论/安全技术/安全审计.md)

### 4. 相关技术
- [Spring AOP](../0104-Spring%20AOP基础.md)
- [表达式语言](../../100-java/100-Java基础/基础/表达式语言.md)
- [反射机制](../../100-java/100-Java基础/反射/反射.md) 