# 消息队列集成

## 重点内容
- Spring消息队列集成架构和核心组件
- JMS和AMQP协议集成
- 消息发送和接收机制
- 消息转换器和序列化
- 事务消息和可靠性保证
- 消息监听器和异步处理

## 消息队列集成概念或介绍

Spring消息队列集成是Spring框架提供的消息中间件集成功能，支持JMS、AMQP等多种消息协议。它提供了统一的消息抽象层，简化了消息队列的使用，支持消息的发送、接收、转换、路由等功能。通过Spring的消息队列集成，开发者可以轻松实现异步消息处理、系统解耦、负载均衡等场景。

### 消息队列集成特点
1. **协议抽象**：统一抽象JMS、AMQP等不同消息协议
2. **消息转换**：支持消息格式转换和序列化
3. **事务支持**：提供事务性消息处理
4. **异步处理**：支持异步消息监听和处理
5. **可靠性保证**：提供消息持久化和重试机制

## 底层原理

### 关键类和接口

#### Message接口
```java
public interface Message {
    
    // 获取消息头
    MessageHeaders getHeaders();
    
    // 获取消息体
    Object getPayload();
    
    // 获取消息ID
    String getMessageId();
    
    // 获取时间戳
    long getTimestamp();
}
```

#### MessageChannel接口
```java
public interface MessageChannel {
    
    // 发送消息
    boolean send(Message<?> message);
    
    // 发送消息（带超时）
    boolean send(Message<?> message, long timeout);
    
    // 获取通道名称
    String getChannelName();
}
```

#### MessageListener接口
```java
public interface MessageListener<T> {
    
    // 处理消息
    void onMessage(Message<T> message);
}
```

### 关键类图

```
MessageProducer
    ↓
MessageChannel
    ↓
MessageConverter
    ↓
MessageListener
    ↓
MessageHandler
    ↓
MessageQueue
```

### 核心代码讲解

#### 1. 消息发送器
```java
@Component
public class MessageSender {
    
    @Autowired
    private JmsTemplate jmsTemplate;
    
    @Autowired
    private RabbitTemplate rabbitTemplate;
    
    public void sendJmsMessage(String destination, Object payload) {
        jmsTemplate.convertAndSend(destination, payload);
    }
    
    public void sendRabbitMessage(String exchange, String routingKey, Object payload) {
        rabbitTemplate.convertAndSend(exchange, routingKey, payload);
    }
    
    public void sendMessageWithHeaders(String destination, Object payload, Map<String, Object> headers) {
        jmsTemplate.convertAndSend(destination, payload, message -> {
            headers.forEach((key, value) -> message.setObjectProperty(key, value));
            return message;
        });
    }
}
```

#### 2. 消息监听器
```java
@Component
public class MessageListener {
    
    @JmsListener(destination = "user.queue")
    public void handleUserMessage(User user) {
        System.out.println("Received user message: " + user);
        // 处理用户消息
    }
    
    @RabbitListener(queues = "order.queue")
    public void handleOrderMessage(Order order) {
        System.out.println("Received order message: " + order);
        // 处理订单消息
    }
    
    @JmsListener(destination = "notification.queue")
    public void handleNotificationMessage(Notification notification) {
        System.out.println("Received notification: " + notification);
        // 处理通知消息
    }
}
```

#### 3. 消息转换器
```java
@Component
public class CustomMessageConverter implements MessageConverter {
    
    private final ObjectMapper objectMapper;
    
    public CustomMessageConverter(ObjectMapper objectMapper) {
        this.objectMapper = objectMapper;
    }
    
    @Override
    public Message<?> toMessage(Object payload, MessageHeaders headers) {
        try {
            String json = objectMapper.writeValueAsString(payload);
            return MessageBuilder.withPayload(json)
                .copyHeaders(headers)
                .build();
        } catch (JsonProcessingException e) {
            throw new MessageConversionException("Failed to convert payload to JSON", e);
        }
    }
    
    @Override
    public Object fromMessage(Message<?> message, Class<?> targetClass) {
        try {
            String json = (String) message.getPayload();
            return objectMapper.readValue(json, targetClass);
        } catch (JsonProcessingException e) {
            throw new MessageConversionException("Failed to convert JSON to object", e);
        }
    }
}
```

### 设计思想

#### 1. 抽象层设计
通过统一的消息抽象层，屏蔽不同消息协议的实现细节。

#### 2. 模板模式
使用Template模式简化消息操作，提供便捷的API。

#### 3. 监听器模式
通过监听器模式实现异步消息处理。

#### 4. 转换器模式
通过转换器模式实现消息格式转换。

## JMS集成

### JMS配置
```java
@Configuration
@EnableJms
public class JmsConfig {
    
    @Bean
    public ConnectionFactory connectionFactory() {
        ActiveMQConnectionFactory factory = new ActiveMQConnectionFactory();
        factory.setBrokerURL("tcp://localhost:61616");
        factory.setTrustAllPackages(true);
        return factory;
    }
    
    @Bean
    public JmsTemplate jmsTemplate(ConnectionFactory connectionFactory) {
        JmsTemplate template = new JmsTemplate(connectionFactory);
        template.setMessageConverter(jacksonJmsMessageConverter());
        return template;
    }
    
    @Bean
    public MessageConverter jacksonJmsMessageConverter() {
        MappingJackson2MessageConverter converter = new MappingJackson2MessageConverter();
        converter.setTargetType(MessageType.TEXT);
        converter.setTypeIdPropertyName("_type");
        return converter;
    }
    
    @Bean
    public DefaultJmsListenerContainerFactory jmsListenerContainerFactory(ConnectionFactory connectionFactory) {
        DefaultJmsListenerContainerFactory factory = new DefaultJmsListenerContainerFactory();
        factory.setConnectionFactory(connectionFactory);
        factory.setMessageConverter(jacksonJmsMessageConverter());
        factory.setConcurrency("3-10");
        factory.setSessionTransacted(true);
        return factory;
    }
}
```

### JMS消息发送
```java
@Service
public class JmsMessageService {
    
    @Autowired
    private JmsTemplate jmsTemplate;
    
    public void sendUserMessage(User user) {
        jmsTemplate.convertAndSend("user.queue", user);
    }
    
    public void sendOrderMessage(Order order) {
        jmsTemplate.convertAndSend("order.queue", order);
    }
    
    public void sendMessageWithHeaders(String destination, Object payload, Map<String, Object> headers) {
        jmsTemplate.convertAndSend(destination, payload, message -> {
            headers.forEach((key, value) -> {
                try {
                    message.setObjectProperty(key, value);
                } catch (JMSException e) {
                    throw new RuntimeException("Failed to set message property", e);
                }
            });
            return message;
        });
    }
    
    public <T> T receiveMessage(String destination, Class<T> targetClass) {
        return jmsTemplate.receiveAndConvert(destination, targetClass);
    }
}
```

### JMS消息监听
```java
@Component
public class JmsMessageListener {
    
    @JmsListener(destination = "user.queue")
    public void handleUserMessage(User user) {
        System.out.println("Received user message: " + user);
        // 处理用户消息
    }
    
    @JmsListener(destination = "order.queue")
    public void handleOrderMessage(Order order) {
        System.out.println("Received order message: " + order);
        // 处理订单消息
    }
    
    @JmsListener(destination = "notification.queue")
    public void handleNotificationMessage(Notification notification) {
        System.out.println("Received notification: " + notification);
        // 处理通知消息
    }
    
    @JmsListener(destination = "error.queue")
    public void handleErrorMessage(String errorMessage) {
        System.err.println("Received error message: " + errorMessage);
        // 处理错误消息
    }
}
```

## AMQP集成

### RabbitMQ配置
```java
@Configuration
@EnableRabbit
public class RabbitConfig {
    
    @Bean
    public ConnectionFactory connectionFactory() {
        CachingConnectionFactory factory = new CachingConnectionFactory();
        factory.setHost("localhost");
        factory.setPort(5672);
        factory.setUsername("guest");
        factory.setPassword("guest");
        return factory;
    }
    
    @Bean
    public RabbitTemplate rabbitTemplate(ConnectionFactory connectionFactory) {
        RabbitTemplate template = new RabbitTemplate(connectionFactory);
        template.setMessageConverter(jacksonMessageConverter());
        return template;
    }
    
    @Bean
    public MessageConverter jacksonMessageConverter() {
        return new Jackson2JsonMessageConverter();
    }
    
    @Bean
    public SimpleRabbitListenerContainerFactory rabbitListenerContainerFactory(ConnectionFactory connectionFactory) {
        SimpleRabbitListenerContainerFactory factory = new SimpleRabbitListenerContainerFactory();
        factory.setConnectionFactory(connectionFactory);
        factory.setMessageConverter(jacksonMessageConverter());
        factory.setConcurrentConsumers(3);
        factory.setMaxConcurrentConsumers(10);
        return factory;
    }
    
    @Bean
    public Queue userQueue() {
        return new Queue("user.queue", true);
    }
    
    @Bean
    public Queue orderQueue() {
        return new Queue("order.queue", true);
    }
    
    @Bean
    public DirectExchange userExchange() {
        return new DirectExchange("user.exchange");
    }
    
    @Bean
    public Binding userBinding(Queue userQueue, DirectExchange userExchange) {
        return BindingBuilder.bind(userQueue).to(userExchange).with("user.routing.key");
    }
}
```

### RabbitMQ消息发送
```java
@Service
public class RabbitMessageService {
    
    @Autowired
    private RabbitTemplate rabbitTemplate;
    
    public void sendUserMessage(User user) {
        rabbitTemplate.convertAndSend("user.exchange", "user.routing.key", user);
    }
    
    public void sendOrderMessage(Order order) {
        rabbitTemplate.convertAndSend("order.exchange", "order.routing.key", order);
    }
    
    public void sendMessageWithHeaders(String exchange, String routingKey, Object payload, Map<String, Object> headers) {
        rabbitTemplate.convertAndSend(exchange, routingKey, payload, message -> {
            headers.forEach((key, value) -> message.getMessageProperties().setHeader(key, value));
            return message;
        });
    }
    
    public <T> T receiveMessage(String queueName, Class<T> targetClass) {
        return rabbitTemplate.receiveAndConvert(queueName, targetClass);
    }
}
```

### RabbitMQ消息监听
```java
@Component
public class RabbitMessageListener {
    
    @RabbitListener(queues = "user.queue")
    public void handleUserMessage(User user) {
        System.out.println("Received user message: " + user);
        // 处理用户消息
    }
    
    @RabbitListener(queues = "order.queue")
    public void handleOrderMessage(Order order) {
        System.out.println("Received order message: " + order);
        // 处理订单消息
    }
    
    @RabbitListener(queues = "notification.queue")
    public void handleNotificationMessage(Notification notification) {
        System.out.println("Received notification: " + notification);
        // 处理通知消息
    }
    
    @RabbitListener(queues = "error.queue")
    public void handleErrorMessage(String errorMessage) {
        System.err.println("Received error message: " + errorMessage);
        // 处理错误消息
    }
}
```

## 消息转换和序列化

### 自定义消息转换器
```java
@Component
public class CustomMessageConverter implements MessageConverter {
    
    private final ObjectMapper objectMapper;
    
    public CustomMessageConverter(ObjectMapper objectMapper) {
        this.objectMapper = objectMapper;
    }
    
    @Override
    public Message<?> toMessage(Object payload, MessageHeaders headers) {
        try {
            String json = objectMapper.writeValueAsString(payload);
            return MessageBuilder.withPayload(json)
                .copyHeaders(headers)
                .setHeader("content-type", "application/json")
                .build();
        } catch (JsonProcessingException e) {
            throw new MessageConversionException("Failed to convert payload to JSON", e);
        }
    }
    
    @Override
    public Object fromMessage(Message<?> message, Class<?> targetClass) {
        try {
            String json = (String) message.getPayload();
            return objectMapper.readValue(json, targetClass);
        } catch (JsonProcessingException e) {
            throw new MessageConversionException("Failed to convert JSON to object", e);
        }
    }
}
```

### 消息序列化配置
```java
@Configuration
public class MessageSerializationConfig {
    
    @Bean
    public ObjectMapper objectMapper() {
        ObjectMapper mapper = new ObjectMapper();
        mapper.registerModule(new JavaTimeModule());
        mapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);
        return mapper;
    }
    
    @Bean
    public MessageConverter jsonMessageConverter(ObjectMapper objectMapper) {
        return new CustomMessageConverter(objectMapper);
    }
}
```

## 事务消息处理

### 事务消息配置
```java
@Configuration
@EnableTransactionManagement
public class TransactionalMessageConfig {
    
    @Bean
    public PlatformTransactionManager transactionManager(DataSource dataSource) {
        return new DataSourceTransactionManager(dataSource);
    }
    
    @Bean
    public JmsTransactionManager jmsTransactionManager(ConnectionFactory connectionFactory) {
        return new JmsTransactionManager(connectionFactory);
    }
}
```

### 事务消息服务
```java
@Service
@Transactional
public class TransactionalMessageService {
    
    @Autowired
    private JmsTemplate jmsTemplate;
    
    @Autowired
    private UserRepository userRepository;
    
    public void createUserWithMessage(User user) {
        // 保存用户到数据库
        userRepository.save(user);
        
        // 发送消息
        jmsTemplate.convertAndSend("user.created", user);
    }
    
    @Transactional(rollbackFor = Exception.class)
    public void processOrderWithMessage(Order order) {
        // 处理订单
        processOrder(order);
        
        // 发送订单消息
        jmsTemplate.convertAndSend("order.processed", order);
    }
    
    private void processOrder(Order order) {
        // 订单处理逻辑
    }
}
```

## 消息可靠性保证

### 消息重试机制
```java
@Configuration
public class MessageReliabilityConfig {
    
    @Bean
    public RetryTemplate retryTemplate() {
        RetryTemplate retryTemplate = new RetryTemplate();
        
        SimpleRetryPolicy retryPolicy = new SimpleRetryPolicy();
        retryPolicy.setMaxAttempts(3);
        retryTemplate.setRetryPolicy(retryPolicy);
        
        ExponentialBackOffPolicy backOffPolicy = new ExponentialBackOffPolicy();
        backOffPolicy.setInitialInterval(1000);
        backOffPolicy.setMultiplier(2.0);
        backOffPolicy.setMaxInterval(10000);
        retryTemplate.setBackOffPolicy(backOffPolicy);
        
        return retryTemplate;
    }
    
    @Bean
    public DefaultJmsListenerContainerFactory reliableJmsListenerContainerFactory(ConnectionFactory connectionFactory) {
        DefaultJmsListenerContainerFactory factory = new DefaultJmsListenerContainerFactory();
        factory.setConnectionFactory(connectionFactory);
        factory.setSessionTransacted(true);
        factory.setConcurrency("3-10");
        factory.setErrorHandler(new MessageListenerErrorHandler());
        return factory;
    }
}
```

### 消息错误处理
```java
@Component
public class MessageListenerErrorHandler implements ErrorHandler {
    
    @Override
    public void handleError(Throwable t) {
        System.err.println("Message processing error: " + t.getMessage());
        // 记录错误日志
        logError(t);
        
        // 发送错误通知
        sendErrorNotification(t);
    }
    
    private void logError(Throwable t) {
        // 实现错误日志记录
    }
    
    private void sendErrorNotification(Throwable t) {
        // 实现错误通知发送
    }
}
```

## 异步消息处理

### 异步消息配置
```java
@Configuration
@EnableAsync
public class AsyncMessageConfig {
    
    @Bean
    public TaskExecutor taskExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(5);
        executor.setMaxPoolSize(10);
        executor.setQueueCapacity(25);
        executor.setThreadNamePrefix("MessageProcessor-");
        executor.initialize();
        return executor;
    }
    
    @Bean
    public AsyncJmsListenerContainerFactory asyncJmsListenerContainerFactory(ConnectionFactory connectionFactory, TaskExecutor taskExecutor) {
        AsyncJmsListenerContainerFactory factory = new AsyncJmsListenerContainerFactory();
        factory.setConnectionFactory(connectionFactory);
        factory.setTaskExecutor(taskExecutor);
        factory.setConcurrency("3-10");
        return factory;
    }
}
```

### 异步消息处理器
```java
@Component
public class AsyncMessageProcessor {
    
    @Async
    public void processUserMessageAsync(User user) {
        System.out.println("Processing user message asynchronously: " + user);
        // 异步处理用户消息
    }
    
    @Async
    public void processOrderMessageAsync(Order order) {
        System.out.println("Processing order message asynchronously: " + order);
        // 异步处理订单消息
    }
    
    @Async
    public CompletableFuture<String> processMessageWithResult(String message) {
        // 异步处理消息并返回结果
        String result = processMessage(message);
        return CompletableFuture.completedFuture(result);
    }
    
    private String processMessage(String message) {
        // 消息处理逻辑
        return "Processed: " + message;
    }
}
```

## 消息队列集成关联的其它知识

### 1. 消息中间件
- [ActiveMQ消息队列](../../300-中间件/activemq.md)
- [RabbitMQ消息队列](../../300-中间件/350-rabbitMQ/rabbitmq.md)
- [Kafka消息队列](../../300-中间件/kafka.md)

### 2. Spring核心
- [Spring IoC容器](../0101-Spring%20IoC容器.md)
- [Spring事务管理](../0108-Spring事务管理.md)
- [Spring异步处理](../0109-Spring异步处理.md)

### 3. 消息协议
- [JMS协议](../../500-基础理论/消息协议/JMS协议.md)
- [AMQP协议](../../500-基础理论/消息协议/AMQP协议.md)
- [消息序列化](../../500-基础理论/消息协议/消息序列化.md)

### 4. 相关技术
- [Java并发编程](../old/100-Java基础-old/多线程/00-Java多线程大纲.md)
- [JSON数据格式](../../200-python/00-python语法/python基础/02-python3%20基本数据类型.md)
- [网络通信](../../500-基础理论/通用计算机知识/一篇搞懂TCP、HTTP、Socket、Socket连接池.md) 