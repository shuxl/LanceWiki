
# 1 重点
- Spring AOP代理机制的核心原理和实现方式
- JDK动态代理和CGLIB代理的详细对比
- 代理对象的创建过程和生命周期管理
- 代理机制的性能优化和最佳实践

# 2 AOP代理机制概念或介绍

## 2.1 代理机制概述
Spring AOP的代理机制是AOP实现的核心，它通过创建代理对象来拦截目标方法的调用，在方法执行前后插入横切关注点的代码。代理机制使得AOP能够在不修改原始代码的情况下实现功能增强。

## 2.2 代理模式在AOP中的应用
代理模式是AOP实现的基础，它提供了以下核心功能：
- **方法拦截**：拦截目标方法的调用
- **通知织入**：在适当的时机执行通知代码
- **透明性**：对调用者透明，无需修改调用代码
- **动态性**：在运行时动态创建代理对象

# 3 代理类型详解

## 3.1 JDK动态代理

### 3.1.1 基本原理
JDK动态代理基于Java的反射机制，通过`java.lang.reflect.Proxy`类创建代理对象。它只能代理实现了接口的类。

```java
// JDK动态代理的核心实现
public class JdkDynamicAopProxy implements AopProxy, InvocationHandler {
    private final AdvisedSupport advised;
    
    public JdkDynamicAopProxy(AdvisedSupport config) {
        this.advised = config;
    }
    
    @Override
    public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {
        // 获取方法拦截器链
        List<Object> chain = this.advised.getInterceptorsAndDynamicInterceptionAdvice(method, this.advised.getTargetClass());
        
        if (chain.isEmpty()) {
            // 没有拦截器，直接调用目标方法
            return method.invoke(this.advised.getTarget(), args);
        } else {
            // 创建方法调用对象
            MethodInvocation invocation = new ReflectiveMethodInvocation(
                proxy, this.advised.getTarget(), method, args, this.advised.getTargetClass(), chain);
            return invocation.proceed();
        }
    }
    
    @Override
    public Object getProxy() {
        return getProxy(ClassUtils.getDefaultClassLoader());
    }
    
    @Override
    public Object getProxy(ClassLoader classLoader) {
        Class<?>[] proxiedInterfaces = AopProxyUtils.completeProxiedInterfaces(this.advised);
        return Proxy.newProxyInstance(classLoader, proxiedInterfaces, this);
    }
}
```

### 3.1.2 代理创建过程
```java
// JDK动态代理的创建过程
public class JdkProxyCreator {
    
    public static Object createProxy(Object target, Class<?>[] interfaces) {
        // 1. 创建InvocationHandler
        InvocationHandler handler = new JdkInvocationHandler(target);
        
        // 2. 创建代理对象
        Object proxy = Proxy.newProxyInstance(
            target.getClass().getClassLoader(),
            interfaces,
            handler
        );
        
        return proxy;
    }
}

class JdkInvocationHandler implements InvocationHandler {
    private Object target;
    private List<MethodInterceptor> interceptors;
    
    public JdkInvocationHandler(Object target) {
        this.target = target;
        this.interceptors = new ArrayList<>();
    }
    
    @Override
    public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {
        // 执行拦截器链
        if (interceptors.isEmpty()) {
            return method.invoke(target, args);
        }
        
        // 创建方法调用
        MethodInvocation invocation = new ReflectiveMethodInvocation(
            proxy, target, method, args, target.getClass(), interceptors);
        return invocation.proceed();
    }
}
```

### 3.1.3 优缺点分析
**优点：**
- 基于Java标准库，无需额外依赖
- 性能相对较好
- 代码简洁，易于理解

**缺点：**
- 只能代理实现了接口的类
- 无法代理final类和方法
- 代理对象必须实现接口

## 3.2 CGLIB代理

### 3.2.1 基本原理
CGLIB（Code Generation Library）通过字节码生成技术，在运行时创建目标类的子类作为代理对象。它可以代理没有实现接口的类。

```java
// CGLIB代理的核心实现
public class CglibAopProxy implements AopProxy {
    private final AdvisedSupport advised;
    
    public CglibAopProxy(AdvisedSupport config) {
        this.advised = config;
    }
    
    @Override
    public Object getProxy() {
        return getProxy(ClassUtils.getDefaultClassLoader());
    }
    
    @Override
    public Object getProxy(ClassLoader classLoader) {
        // 创建Enhancer
        Enhancer enhancer = new Enhancer();
        enhancer.setSuperclass(this.advised.getTargetClass());
        enhancer.setInterfaces(this.advised.getProxiedInterfaces());
        enhancer.setCallback(new CglibMethodInvocation());
        
        // 生成代理类
        return enhancer.create();
    }
}

class CglibMethodInvocation implements MethodInterceptor {
    private final AdvisedSupport advised;
    
    public CglibMethodInvocation(AdvisedSupport advised) {
        this.advised = advised;
    }
    
    @Override
    public Object intercept(Object obj, Method method, Object[] args, MethodProxy proxy) throws Throwable {
        // 获取拦截器链
        List<Object> chain = this.advised.getInterceptorsAndDynamicInterceptionAdvice(method, this.advised.getTargetClass());
        
        if (chain.isEmpty()) {
            // 没有拦截器，直接调用目标方法
            return proxy.invokeSuper(obj, args);
        } else {
            // 创建方法调用对象
            MethodInvocation invocation = new CglibMethodInvocation(
                obj, this.advised.getTarget(), method, args, this.advised.getTargetClass(), chain, proxy);
            return invocation.proceed();
        }
    }
}
```

### 3.2.2 代理创建过程
```java
// CGLIB代理的创建过程
public class CglibProxyCreator {
    
    public static Object createProxy(Object target) {
        // 1. 创建Enhancer
        Enhancer enhancer = new Enhancer();
        enhancer.setSuperclass(target.getClass());
        enhancer.setCallback(new CglibInvocationHandler(target));
        
        // 2. 生成代理对象
        return enhancer.create();
    }
}

class CglibInvocationHandler implements MethodInterceptor {
    private Object target;
    private List<MethodInterceptor> interceptors;
    
    public CglibInvocationHandler(Object target) {
        this.target = target;
        this.interceptors = new ArrayList<>();
    }
    
    @Override
    public Object intercept(Object obj, Method method, Object[] args, MethodProxy proxy) throws Throwable {
        // 执行拦截器链
        if (interceptors.isEmpty()) {
            return proxy.invokeSuper(obj, args);
        }
        
        // 创建方法调用
        MethodInvocation invocation = new CglibMethodInvocation(
            obj, target, method, args, target.getClass(), interceptors, proxy);
        return invocation.proceed();
    }
}
```

### 3.2.3 优缺点分析
**优点：**
- 可以代理没有实现接口的类
- 性能在某些场景下优于JDK动态代理
- 支持final方法的代理（通过继承）

**缺点：**
- 需要额外的CGLIB依赖
- 无法代理final类
- 生成的代理类会继承目标类，可能影响继承结构

# 4 代理选择机制

## 4.1 代理选择策略
Spring AOP根据以下规则选择代理类型：

```java
public class DefaultAopProxyFactory implements AopProxyFactory {
    
    @Override
    public AopProxy createAopProxy(AdvisedSupport config) {
        // 检查是否强制使用CGLIB
        if (config.isOptimize() || config.isProxyTargetClass() || hasNoUserSuppliedProxyInterfaces(config)) {
            Class<?> targetClass = config.getTargetClass();
            if (targetClass == null) {
                throw new AopConfigException("TargetSource cannot determine target class");
            }
            
            // 如果目标类是接口或者是代理类，使用JDK动态代理
            if (targetClass.isInterface() || Proxy.isProxyClass(targetClass)) {
                return new JdkDynamicAopProxy(config);
            }
            
            // 否则使用CGLIB代理
            return new CglibAopProxy(config);
        } else {
            // 默认使用JDK动态代理
            return new JdkDynamicAopProxy(config);
        }
    }
}
```

## 4.2 配置控制
```java
// 通过配置控制代理类型
@Configuration
@EnableAspectJAutoProxy(proxyTargetClass = true) // 强制使用CGLIB
public class AopConfig {
    // 配置内容
}

// 或者在XML中配置
<aop:aspectj-autoproxy proxy-target-class="true"/>
```

# 5 代理对象生命周期

## 5.1 创建阶段
```java
// 代理对象的创建过程
public class ProxyFactory {
    
    public Object getProxy() {
        // 1. 创建AdvisedSupport
        AdvisedSupport advisedSupport = new AdvisedSupport();
        advisedSupport.setTarget(target);
        advisedSupport.setInterfaces(interfaces);
        
        // 2. 添加通知
        for (Advisor advisor : advisors) {
            advisedSupport.addAdvisor(advisor);
        }
        
        // 3. 创建代理
        AopProxy aopProxy = createAopProxy(advisedSupport);
        return aopProxy.getProxy();
    }
}
```

## 5.2 初始化阶段
```java
// 代理对象的初始化
public class ProxyInitializer implements BeanPostProcessor {
    
    @Override
    public Object postProcessAfterInitialization(Object bean, String beanName) {
        // 检查是否需要创建代理
        if (shouldCreateProxy(bean, beanName)) {
            return createProxy(bean);
        }
        return bean;
    }
    
    private boolean shouldCreateProxy(Object bean, String beanName) {
        // 检查是否有匹配的切点
        return hasMatchingPointcut(bean, beanName);
    }
}
```

## 5.3 销毁阶段
```java
// 代理对象的销毁
public class ProxyDestroyer implements DisposableBean {
    
    @Override
    public void destroy() {
        // 清理代理相关的资源
        cleanupProxyResources();
    }
}
```

# 6 性能优化机制

## 6.1 代理缓存
```java
// 代理对象缓存
public class ProxyCache {
    private static final Map<Class<?>, Object> proxyCache = new ConcurrentHashMap<>();
    
    public static Object getProxy(Object target) {
        Class<?> targetClass = target.getClass();
        
        return proxyCache.computeIfAbsent(targetClass, k -> {
            return createProxy(target);
        });
    }
    
    public static void clearCache() {
        proxyCache.clear();
    }
}
```

## 6.2 切点表达式缓存
```java
// 切点表达式缓存
public class PointcutCache {
    private static final Map<String, PointcutExpression> expressionCache = new ConcurrentHashMap<>();
    
    public static PointcutExpression getPointcutExpression(String expression) {
        return expressionCache.computeIfAbsent(expression, k -> {
            return PointcutParser.getPointcutParserSupportingAllPrimitivesAndUsingContextClassloaderForResolution()
                    .parsePointcutExpression(expression);
        });
    }
}
```

## 6.3 方法匹配缓存
```java
// 方法匹配结果缓存
public class MethodMatchCache {
    private static final Map<Method, Boolean> matchCache = new ConcurrentHashMap<>();
    
    public static boolean matches(Method method, Pointcut pointcut) {
        return matchCache.computeIfAbsent(method, k -> {
            return pointcut.matches(method, method.getDeclaringClass());
        });
    }
}
```

# 7 代理机制的关键类

## 7.1 AopProxy接口
```java
public interface AopProxy {
    Object getProxy();
    Object getProxy(ClassLoader classLoader);
}
```

## 7.2 AdvisedSupport类
```java
public class AdvisedSupport extends ProxyConfig implements Advised {
    private List<Advisor> advisors = new ArrayList<>();
    private Object target;
    private Class<?>[] interfaces;
    private boolean proxyTargetClass = false;
    
    public void addAdvisor(Advisor advisor) {
        advisors.add(advisor);
    }
    
    public List<Advisor> getAdvisors() {
        return advisors;
    }
    
    public Object getTarget() {
        return target;
    }
    
    public void setTarget(Object target) {
        this.target = target;
    }
}
```

## 7.3 MethodInvocation接口
```java
public interface MethodInvocation {
    Method getMethod();
    Object[] getArguments();
    Object getThis();
    Object proceed() throws Throwable;
}
```

# 8 设计思想

## 8.1 代理模式的应用
Spring AOP基于代理模式，通过代理对象拦截方法调用，实现横切关注点的处理。

## 8.2 策略模式
根据目标类的特征选择不同的代理策略（JDK动态代理或CGLIB代理）。

## 8.3 责任链模式
多个拦截器通过责任链模式组织，按顺序执行。

## 8.4 工厂模式
使用代理工厂创建不同类型的代理对象。

# 9 AOP代理机制关联的其它知识

## 9.1 Spring框架核心
- **[Spring AOP基础概念](0301-AOP基础概念.md)**：代理机制的基础概念
- **[Spring AOP实现原理](0302-Spring%20AOP实现原理.md)**：代理机制的实现原理
- **[AOP切面类型](0303-AOP切面类型.md)**：代理机制支持的切面类型

## 9.2 设计模式
- **代理模式**：AOP代理机制的核心
- **策略模式**：代理类型的选择策略
- **责任链模式**：拦截器链的执行机制
- **工厂模式**：代理对象的创建

## 9.3 字节码技术
- **ASM**：CGLIB的底层字节码操作
- **Javassist**：另一种字节码操作库
- **字节码生成**：动态代理类的生成

## 9.4 反射机制
- **Method.invoke()**：JDK动态代理的核心
- **MethodProxy**：CGLIB代理的核心
- **反射性能**：代理调用的性能考虑

## 9.5 性能优化
- **代理性能对比**：不同代理类型的性能差异
- **缓存机制**：代理对象的缓存策略
- **内存优化**：代理对象的内存管理

## 9.6 最佳实践
- **代理类型选择**：根据场景选择合适的代理类型
- **性能优化**：代理机制的性能优化策略
- **内存管理**：代理对象的内存管理策略
- **调试技巧**：代理对象的调试方法 